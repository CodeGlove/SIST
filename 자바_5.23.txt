스레드: 독립된 최소 실행단위

스레드가 여러개: 멀티스레드

스레드는 자기 할 일이 끝나면 소멸

스레드 생성: Thread t1 = new Thread();

t1.start(); //일 준비! ->해당객체 run함수 수행 ->일이 끝나면 소멸

Thread객체에 run함수가 있다

스레드 객체를 실행시키고 싶으면 run함수를 재정의 해야 한다.

그냥 두면 빈 객체이기때문에 아무것도 안하고 소멸된다.




