Call by value 

Call by Reference

getter, setter 필요한 이유: 멤버변수를 private으로해놔서

부모클래스=슈퍼클래스=베이스클래스 전부 같은말.

extends를 적지 않으면 전부 object로부터 상속받은 객체다. 

if(a==b): 주소를 비교
if(a.equals(b)): a와 b의 내용을 비교

메서드 재정의: 부모와 자식간에 상속이 이루어진 상태에서 자식이 부모와 똑같은 함수를 가지고있을때 부모의 함수는 숨김이 일어난다. 우선순위는 자식이 먼저